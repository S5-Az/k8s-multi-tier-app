kind: Service
apiVersion: v1
metadata:
  name: backend-service
  namespace: project1
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
